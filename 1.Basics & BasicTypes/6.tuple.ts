let skill: [string, number];
skill = ['Programming', 5];
 

 //we also can't add more data in tuple array like 
 //=> skill = ['Programming', 5,"more data"];
//but push his something else typescript can't catch this. 
//=> skill.push(3)

console.log(skill)
